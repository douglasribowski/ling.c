#include <stdio.h>
    int main(){ int contador=0;
        while(contador<3)
        {
            printf("contador=%d\n",contador);
            contador++;
        }
        printf("acabou!!!!");
    }
